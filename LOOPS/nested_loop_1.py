for left in range(7):
    for right in range(left, 7):
        print("[" + str(left) + "|" + str(right) + "]", end=" ")
        # parameter: "end=" to replace the default newline parameter
    print()